import BuilderButton from "./BuilderButton";

export default function ExperienceButton({ onButtonClick }) {
  return (
    <BuilderButton ButtonText="Work Experience" onButtonClick={onButtonClick} />
  );
}
